{
  "name": "waba-bot",
  "version": "0.0.1",
  "description": "",
  "author": "Natanael Weslley",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "nest build",
    "format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\"",
    "services:up": "docker compose -f compose.yml up -d",
    "services:stop": "docker compose -f compose.yml stop",
    "services:down": "docker compose -f compose.yml down",
    "start": "pnpm run services:up && pnpm run wait-for-postgres && pnpm run migrate:deploy && nest start",
    "start:dev": "pnpm run services:up && pnpm run wait-for-postgres && pnpm run migrate:deploy && pnpm run start --watch",
    "start:debug": "pnpm run services:up && pnpm run wait-for-postgres && pnpm run migrate:deploy && pnpm run start --debug --watch",
    "start:prod": "node dist/main",
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix",
    "test": "pnpm run services:up && pnpm run wait-for-postgres && pnpm run migrate:deploy && jest --runInBand",
    "test:watch": "pnpm run services:up && pnpm run wait-for-postgres && pnpm run migrate:deploy && jest --watchAll --runInBand",
    "test:cov": "pnpm run services:up && pnpm run wait-for-postgres && pnpm run migrate:deploy && jest --coverage --runInBand",
    "test:debug": "pnpm run services:up && pnpm run wait-for-postgres && pnpm run migrate:deploy && node --inspect-brk -r tsconfig-paths/register -r ts-node/register node_modules/.bin/jest --runInBand",
    "test:e2e": "pnpm run services:up && pnpm run wait-for-postgres && pnpm run migrate:deploy && jest --config ./test/jest-e2e.json --runInBand",
    "migrate:make": "prisma migrate dev --name",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:status": "prisma migrate status",
    "wait-for-postgres": "node scripts/wait-for-postgres.js"
  },
  "dependencies": {
    "@nestjs/bull": "^11.0.4",
    "@nestjs/common": "^11.0.1",
    "@nestjs/core": "^11.0.1",
    "@nestjs/mapped-types": "^2.1.0",
    "@nestjs/platform-express": "^11.0.1",
    "@nestjs/swagger": "^11.2.3",
    "@prisma/adapter-pg": "^7.2.0",
    "@prisma/client": "^7.2.0",
    "bull": "^4.16.5",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.3",
    "pg": "^8.16.3",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.2.0",
    "@eslint/js": "^9.18.0",
    "@nestjs/cli": "^11.0.0",
    "@nestjs/schematics": "^11.0.0",
    "@nestjs/testing": "^11.0.1",
    "@types/express": "^5.0.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^22.10.7",
    "@types/supertest": "^6.0.2",
    "dotenv": "^17.2.3",
    "eslint": "^9.18.0",
    "eslint-config-prettier": "^10.0.1",
    "eslint-plugin-prettier": "^5.2.2",
    "globals": "^16.0.0",
    "jest": "^30.0.0",
    "jest-mock-extended": "^4.0.0",
    "prettier": "^3.4.2",
    "prisma": "^7.2.0",
    "source-map-support": "^0.5.21",
    "supertest": "^7.0.0",
    "ts-jest": "^29.2.5",
    "ts-loader": "^9.5.2",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.20.0"
  },
  "jest": {
    "moduleFileExtensions": [
      "js",
      "json",
      "ts"
    ],
    "rootDir": "src",
    "testRegex": ".*\\.spec\\.ts$",
    "transform": {
      "^.+\\.(t|j)s$": "ts-jest"
    },
    "collectCoverageFrom": [
      "**/*.(t|j)s"
    ],
    "coverageDirectory": "../coverage",
    "testEnvironment": "node"
  }
}